apiVersion: v1
kind: Service
metadata:
  name: mt-web-prod
# spec:
#   type: NodePort
#   selector:
#     app: web
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80
spec:
  selector:
    app: mt-web-prod
  #type: LoadBalancer
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
