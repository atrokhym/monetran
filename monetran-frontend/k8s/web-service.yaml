apiVersion: v1
kind: Service
metadata:
  name: mt-web
# spec:
#   type: NodePort
#   selector:
#     app: web
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80
spec:
  selector:
    app: mt-web
  #type: LoadBalancer
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
